version: "3.9"

services:

  rag:

    build: .

    container_name: ai_research_assistant

    ports:

      - "8501:8501"

      - "8000:8000"

    env_file:

      - .env

    volumes:

      - .:/app